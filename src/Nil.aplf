 Nil←{
   ⍝ rc ← [ns] dfn Nil name
   ⍝ - Creates a niladic function <name> that, when called, will return <dfn ⍬>.
   ⍝ - Places <name> in the namespace <ns> or, if ⍺ is omitted, the namespace Nil was called from.
   ⍝ - Nil returns 1 if successful, else 0.
     ⍺←⊃⎕RSI ⋄ ⍙←⍺⍺ ⋄ call←(⊂'⍙←',⍵),⎕NR'⍙' ⋄ (⊃⌽call),←'⍬'
     1:_←0≠⊃0⍴⍺.⎕FX call
 }
