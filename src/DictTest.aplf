 DictTest←{
     ⎕PW←200
     range←⍵

     _←'cmpx'⎕CY'dfns'

     SIZE←⌈10*0 0.3 0.5 1 1.5 2 3 3.5 4
     NAMES←'X',∘⍕¨SIZE

     KEYS←↓⎕A[?10001 8⍴≢⎕A]

     d∘←0 Dict(¯1↓KEYS)(↓⎕A[?10000 8⍴≢⎕A])
     ⎕←'range: 0..',(range-1)
     _←⍎¨NAMES{⍺,'∘← KEYS[10000⌊?',(⍕⍵),'⍴',(⍕range),']'}¨SIZE
     ⎕←'cmpx...'
     ⎕←opts←((⊂'d.GetX '),¨NAMES),(⊂'d.Get¨ '),¨NAMES
     ⎕←cmpx opts
     ⎕←39 '¯'
     ⍬
 }
